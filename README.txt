Download node.js , NPM

type the code in the VS Code Terminal:

1. npm install = for download all package for App
2. npm run dev = run a Tailwind CSS Library 
3. run project by live server! (go live)
